<!DOCTYPE html>
<html>
<head>
  <title>Bloccit</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<div class="container">
  <ul class="nav nav-tabs">
    <li><%= link_to 'Bloccit', root_path %></li>
    <li><%= link_to 'Topics', topics_path %></li>
    <li><%= link_to 'About', about_path %></li>
    <div class="pull-right user-info">
      <% if current_user %>
            <%= image_tag avatar_url(current_user), class: 'pull-right img-size' %>
            <div  class="pull-right text-style">
              <%= current_user.name %> <br>
              <%= link_to 'Sign Out', session_path(current_user), method: :delete %>
            </div>
      <% else %>
          <%= link_to 'Sign In', new_session_path %> or
          <%= link_to 'Sign up', new_user_path %>
      <% end %>
      </div>
  </ul>
  <%= yield %>
</div>
</body>
</html>
